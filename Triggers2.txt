CREATE OR REPLACE FUNCTION trigger2()
  RETURNS trigger AS
$BODY$
BEGIN
 IF T_Rate< 30 THEN
 INSERT INTO Available_Tests VALUES(50, T_Name, T_ID);
 END IF;
 RETURN NEW;
END;
$BODY$
LANGUAGE plpgsql VOLATILE
COST 100;